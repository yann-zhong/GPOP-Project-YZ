# Necessary imports
import numpy as np
import matplotlib.pyplot as plt
import matplotlib.animation as animation
import statistics

p = 0.6
q = 1-p
generations = 750
population = 100
subpopulation_nb = 10

# function to generate set nb of subpopulations from initial population size and simulate on them
def clonal_subpopulations(p,q,generations,population,subpopulation_nb):

    p_subpopulation = p*(population/subpopulation_nb)
    q_population = populatio9n - p_subpopulation
    whole_subpop_dist = list(np.zeros(population/subpopulation_nb,subpopulation_nb))
    
    return 0

print(5)